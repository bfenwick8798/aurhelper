# AUR GitHub Mirror Helper
A simple script to install packages from the AUR mirror on GitHub during frontend outages which may affect AUR helpers like yay.

## Usage:
`aurmirrorinstaller.sh <package-name>`